# SQLAlchemy 1.4 constraints file
# Use this when testing with pyathena/athena dependencies that require SQLAlchemy < 2.0
#
# Usage:
#   pip install -c constraints-dev-sqlalchemy1.txt -r requirements.txt [other requirements...]
#
# This file should be used for:
# - athena marker tests
# - any tests that specifically need SQLAlchemy 1.4 compatibility


# Ensure greenlet is compatible with SQLAlchemy 1.4
greenlet!=0.4.17

# Pandas compatibility with SQLAlchemy 1.4
# Note: pandas 2.x requires some careful handling with SQLAlchemy 1.4
pandas>=1.3.0,<2.2
# Pin SQLAlchemy to 1.4.x series for compatibility with pyathena
sqlalchemy>=1.4.0,<2.0.0
